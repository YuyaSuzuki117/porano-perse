{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(echo $CLAUDE_TOOL_INPUT | jq -r '.file_path // empty'); if [ -n \"$FILE\" ] && echo \"$FILE\" | grep -qE '\\.(tsx?|jsx?)$'; then cd C:/Users/LENOVO/.gemini/porano-perse && npx tsc --noEmit --pretty 2>&1 | head -30; fi",
            "timeout": 30000
          }
        ]
      }
    ]
  }
}
